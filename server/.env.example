# Copy to .env and fill in. Without GEMINI_API_KEY the broker runs in MOCK mode
# (scripted narration) so the whole app still works end-to-end for local dev/demos.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash-live-001
PORT=8787
